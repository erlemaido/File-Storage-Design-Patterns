@model WebApp.ViewModels.ProductViewModel

@{
    ViewData["Title"] = "Delete";
}

<h1>Kustuta</h1>

<h3>Kas oled kindel, et soovid kustutada?</h3>
<div>
    <h4>Toode</h4>
    <hr/>
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.ProductCode)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.ProductCode)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Title)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Title)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Price)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Price) €
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.ProductStateType)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.ProductStateType.Title)
        </dd>
        <dt>
            @{
                var pictures = Model.PictureUrls;
                @foreach (var picture in pictures)
                {
                    <img src="@picture" height="300" width="300" asp-append-version="true" alt=""/>
                }
            }
        </dt>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="ProductId"/>
        <input type="submit" value="Kustuta" class="btn btn-danger"/> |
        <a asp-action="Index">Tagasi</a>
    </form>
</div>